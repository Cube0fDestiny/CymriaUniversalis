jot1 = {
	slot = 1
	generic = no
	ai = yes
	potential = {
		tag = GI1
		NOT = { map_setup = map_setup_random }
	}
	has_country_shield = yes
	
	the_wolf_conclave = {
		icon = mission_build_up_to_force_limit
		required_missions { }
		position = 1
		trigger = {
			prestige = 15
		}
		effect = {
			add_stability = 1
		}
	}
	
	hunt_dissidents = {
		icon = mission_build_up_to_force_limit
		required_missions {
			the_wolf_conclave
		}
		position = 2
		trigger = {
			adm_power = 100
		}
		effect = {
			add_adm_power = -100
			add_ruler_modifier = {
				name = werwolf_support
				duration = -1
			}
		}
	}
}

jot2 = {
	slot = 2
	generic = no
	ai = yes
	potential = {
		tag = GI1
		NOT = { map_setup = map_setup_random }
	}
	has_country_shield = yes
	
	smelt_iron = {
		icon = mission_build_up_to_force_limit
		required_missions {
			the_giants_are_preparing
		}
		position = 2
		provinces_to_highlight = {
			trade_goods = iron
		}
		trigger = {
			iron = 4
		}
		effect = {
			add_army_tradition = 10
		}
	}
	
	muspelhjeim = {
		icon = mission_build_up_to_force_limit
		required_missions {
			preparing_for_war
		}
		position = 4
		provinces_to_highlight = {
			area = muspelland_area
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			muspelland_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}			
		}
		effect = {
			add_splendor = 200
		}
	}
}

jot3 = {
	slot = 3
	generic = no
	ai = yes
	potential = {
		tag = GI1
		NOT = { map_setup = map_setup_random }
	}
	has_country_shield = yes
	
	the_giants_are_preparing = {
		icon = mission_build_up_to_force_limit
		required_missions { }
		position = 1
		trigger = {
			stability = 1
			army_size_percentage = 1
		}
		effect = {
			add_prestige = 25
		}
	}
	
	in_jotunheim = {
		icon = mission_build_up_to_force_limit
		required_missions {
			the_giants_are_preparing
		}
		position = 2
		provinces_to_highlight = {
			province_id = 3500
		}
		trigger = {
			3500 = { development = 30 }
		}
		effect = {
			3500 = { add_prosperity = 100 }
		}
	}
	
	preparing_for_war = {
		icon = mission_build_up_to_force_limit
		required_missions {
			in_jotunheim
		}
		position = 3
		trigger = {
			army_size = 15
		}
		effect = {
			muspelland_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_claim = ROOT
			}
			niefelland_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_claim = ROOT
			}
		}
	}
	
	unite_the_giantsland = {
		icon = mission_build_up_to_force_limit
		required_missions {
			niefelheim
			muspelhjeim
		}
		position = 5
		provinces_to_highlight = {
			OR = {
				owned_by = GI2
				owned_by = GI3
				culture_group = ROOT
			}
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			NOT = { exists = GI2 }
			NOT = { exists = GI3 }
			has_unified_culture_group = yes
		}
		effect = {
			jotunheim_region = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_claim = ROOT
			}
		}
	}
	
	united_under_a_banner = {
		icon = mission_build_up_to_force_limit
		required_missions {
			unite_the_giantsland
		}
		position = 6
		trigger = {
			stability = 3
			total_development = 250
		}
		effect = {
			change_tag = GI4
			swap_non_generic_missions = yes
		}
	}
}

jot4 = {
	slot = 4
	generic = no
	ai = yes
	potential = {
		tag = GI1
		NOT = { map_setup = map_setup_random }
	}
	has_country_shield = yes
	
	gather_taxes = {
		icon = mission_build_up_to_force_limit
		required_missions {
			the_giants_are_preparing
		}
		position = 2
		trigger = {
			dip_power = 150
		}
		effect = {
			add_dip_power = -150
			add_ruler_modifier = {
				name = war_Taxes
				duration = -1
			}
			add_years_of_income = 1
		}
	}
	
	niefelheim = {
		icon = mission_build_up_to_force_limit
		required_missions {
			preparing_for_war
		}
		position = 4
		provinces_to_highlight = {
			area = niefelland_area
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
		}
		trigger = {
			niefelland_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}	
		}
		effect = {
			add_yearly_manpower = 1
		}
	}
}

jot5 = {
	slot = 5
	generic = no
	ai = yes
	potential = {
		tag = GI1
		NOT = { map_setup = map_setup_random }
	}
	has_country_shield = yes
	
	the_jarl_thing = {
		icon = mission_build_up_to_force_limit
		required_missions { }
		position = 1
		trigger = {
			splendor = 150
		}
		effect = {
			add_splendor = -150
			add_army_professionalism = 0.05
		}
	}
	
	jarl_leading = {
		icon = mission_build_up_to_force_limit
		required_missions {
			the_jarl_thing
		}
		position = 2
		trigger = {
			has_leader_with = {
				total_pips = 10
			}
		}
		effect = {
			add_army_tradition = 15
		}
	}
}